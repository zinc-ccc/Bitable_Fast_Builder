# HRBP 周会系统搭建规则

> ⚠️ **AI 执行守则**：
> - 本文档是 HRBP 周会系统的权威规则文件，AI 必须忠实读取并执行，不得自行解读或简化。
> - **未经用户明确允许，禁止修改本文档任何内容。**
> - 如发现本文档与对话中的新要求存在冲突，须主动提示用户："当前操作与规则文档存在差异，是否需要更新规则文档？"
> - 每次涉及周会系统相关操作前，应优先读取本文档作为执行依据。

---

# HRBP周报系统自动化搭建方案规划书 (V3.0_Final)

## 一、 系统基本信息
- **负责人**：Hannah.Wei
- **底表名称**：`BP配置中心` (已存在，包含：人员ID、组别、群聊ID等)
- **主表名称**：`HRBP周报` (由 API 新建或手动新建)
- **核心逻辑**：BP 填写敏感原文 -> API 异步生成去敏摘要 -> 负责人 Hannah 筛选点名 -> 周会分组投屏。

---

## 二、 字段配置明细表 (主表: HRBP周报)

请严格执行以下命名规范（字段内部使用下划线，禁止括号/横杠/点号，符号统一使用半角英文）。

| 序号 | 字段名称 | 字段类型 | 详细配置 / 逻辑说明 | 生成方 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **填写日期** | 创建日期 | 自动生成。显示格式：2026_02_25 | 系统自动 |
| 2 | **归档标识** | 文本 | **API 写入**：`YYYY_MM` (用于按月筛选) | API |
| 3 | **周索引** | 文本 | **API 写入**：`Wxx` (ISO标准周序，如 W09) | API |
| 4 | **汇报人** | 人员 | 表单设置：自动采集提交人账号 | 系统自动 |
| 5 | **底表关联** | 关联 | 关联至 `BP配置中心` | **API 回填** |
| 6 | **所属小组** | 查找引用 | 引用 `底表关联` 中的 `组别` 字段 | 系统自动 |
| 7 | **招聘产出与HC确认** | 长文本 | BP 填报原文 | BP |
| 8 | **摘要_招聘** | 文本 | **API 写入**：AI 提炼 15_20 字摘要 | API |
| 9 | **需汇报_招聘** | 复选框 | 复选框 | BP/Hannah |
| 10 | **Agent实践与进展** | 长文本 | BP 填报原文 | BP |
| 11 | **摘要_Agent** | 文本 | **API 写入**：AI 提炼 15_20 字摘要 | API |
| 12 | **需汇报_Agent** | 复选框 | 复选框 | BP/Hannah |
| 13 | **人员异常与试用期** | 长文本 | BP 填报原文 | BP |
| 14 | **摘要_人员** | 文本 | **API 写入**：AI 提炼 15_20 字摘要 | API |
| 15 | **需汇报_人员** | 复选框 | 复选框 | BP/Hannah |
| 16 | **业务部门情况反馈** | 长文本 | BP 填报原文 | BP |
| 17 | **摘要_业务** | 文本 | **API 写入**：AI 提炼 15_20 字摘要 | API |
| 18 | **需汇报_业务** | 复选框 | 复选框 | BP/Hannah |
| 19 | **下周计划与卡点** | 长文本 | **对接预留**：用于后续 AI 自动提取待办事项 | BP |
| 20 | **摘要_计划** | 文本 | **API 写入**：AI 提炼 15_20 字摘要 | API |
| 21 | **需汇报_计划** | 复选框 | 复选框 | BP/Hannah |
| 22 | **议程建议** | 文本 | **API 写入**：AI 综合判断给出的推荐理由 | API |

---

## 三、 权限隔离方案 (手动配置指引)

### 1. 记录级权限 (BP 隐私隔离)
- **设置角色**：新建 `HRBP_Team` 角色。
- **配置逻辑**：勾选“记录级权限”，条件设为 `创建人` **等于** `当前用户`。
- **效果**：BP 只能看到自己提交的详细 M 模块内容。

### 2. 负责人权限 (全局视角)
- **角色成员**：Hannah.Wei。
- **配置逻辑**：设置为多维表管理员，拥有所有记录的查看与编辑权限。

---

## 四、 视图展示设计 (看板配置)

### 1. 视图名称：`本周议程点位` (画册视图)
- **筛选**：`填写日期` 属于 `本周`。
- **封面展示**：`汇报人` + 各模块 `摘要_xxx` + 各模块 `需汇报_xxx`。
- **用途**：Hannah 在此视图进行周会前的快速“点兵点将”。

### 2. 视图名称：`周会投屏_分组看板` (看板视图)
- **筛选**：任一 `需汇报_xxx` 为“已勾选”。
- **分组依据**：按 `所属小组` 字段呈现。
- **显示控制**：**隐藏** M1-M5 原文字段，仅展示 `汇报人` 和 `摘要_xxx`。
- **用途**：周会投屏，按小组拉通摘要，保护原文隐私。

---

## 五、 自动化与 API 核心逻辑

### 1. 自动映射逻辑 (API)
- 监听到主表新增记录 -> 抓取 `汇报人` UserID -> 在 `BP配置中心` 检索匹配 `人员ID` -> 将 Record_ID 回填至主表 `底表关联`。

### 2. 重点讨论通知 (自动化流)
- **触发条件**：`需汇报_xxx` 变更为“已勾选” 且 修改人 **等于** `Hannah.Wei`。
- **动作**：发送机器人私聊给 `汇报人`。
- **文案**：`你的周报中 [字段名] 被列入今晚重点讨论，请准备说明并注意控制时长，感谢~`

---

## 六、 未来扩展预留
- **下周计划与卡点**：字段已独立，方便后续 API 读取并自动生成待办任务。
# FJD 丰疆智能 HRBP 周报自动化协议 (V11.0)

## 1. 核心人格与背景
- 你是【丰疆智能 FJD】的资深 HR 专家。
- 业务重心：机器人/智能制造、全球化出海、AI 提效组织转型。
- 语言规范：所有写入内容使用半角英文符号，字段连接使用下划线。

## 2. 自动化执行逻辑 (自适应匹配)
请编写脚本，动态获取当前表格的所有字段名，并按以下逻辑执行：

- **时间维度自动打标**：
  获取 `填写日期`，在对应的 `归档标识` 写入 **YYYY_MM**，在 `周索引` 写入 **Wxx**。

- **AI 摘要自动映射 (原文 -> 摘要)**：
  自动匹配所有“业务模块字段”及其对应的“摘要字段”。
  逻辑：读取 `[模块名]` 的原文内容，提炼 15-20 字专家级摘要，写入 `摘要_[模块名]` 字段。
  *示例：读取 [招聘产出与HC确认] -> 写入 [摘要_招聘]*。

- **决策支持**：
  综合全表所有原文内容，在 `议程建议` 字段中为 Hannah 提供本周周会最值得关注的 1 个风险点或点名理由。

- **需汇报联动**：
  若 AI 判断原文中存在重大卡点或风险（如离职、投诉等），需自动将对应的 `需汇报_[模块名]` 复选框勾选。

## 3. 技术要求
- **动态性**：不要写死字段码，通过字段名进行模糊匹配或语义匹配。
- **稳定性**：若 BP 某项未填，摘要应显示“本周无变动”。
- **API 配置**：预留 Base_URL 和 API_KEY 的环境变量接口。